- [x] 图片详情页点击地图会自动缩放到固定大小，需要修改；手动填经纬度会出现0填不了，需要修改这个数值验证逻辑。
- [x] 用户新创建时，有的部分页面会显示两个提示配置oss的提示条，现在统一设置成每个页面顶端显示一遍这个提示信息。并且这个信息的颜色需要适配深浅两种模式。
- [ ] 新增：除了oss存储图片数据本身，图片之外的所有关于该用户图片的配置信息，例如标签、手动位置、描述信息等等，都需要支持与oss备份，并且执行同步。相关配置信息存储的路径的修改也支持在设置页面修改，点击执行同步按钮时，会自动同步所有用户图片oss存储的图片信息，并同步该用户所有图片所对应的所有的配置信息。