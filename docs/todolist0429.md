- [x] 上传照片页面选择照片后显示在队列里，不要立刻上传，支持对队列中的待上传照片进行叉号删除操作，确认无误之后，需要前端有一个上传按钮，点击之后再上传至oss。
- [x] 标签管理页面支持对标签【增、删、改】操作、用户管理页面（管理员）支持【删除】用户账号。所有的删除功能需要添加重要的二级弹窗确认！管理员还要在用户页面支持开放注册按钮，如果开放注册的状态，login页面则开放一个注册功能，如果管理员关闭注册，则该功能消失。
- [ ] 上传页面：图片位于上传队列中，处于待上传状态时，除了支持图片从队列中删除外，还要支持单独对该图片修改标签。如果在上方设置，则默认统一给这批上传的图片添加该标签或取消打标签，下面的队列中每个图片的标签状态也要实时变化。
- [ ] 点击图片缩略图打开图片的详情页，需要是一个单独的界面铺满全屏，而不是悬浮窗的形式。从缩略图到详情页的图片加一个图片放大的动画：从当前缩略图的位置扩展至详情页图片的位置。 详情页图片后面的背景用这个图片的高斯磨砂玻璃模糊当成背景。图片需要支持拖动和滚轮缩放。详情页按键盘esc按键或者右上角的关闭按钮关闭照片详情页。
- [ ] 图片点开详情页，右侧支持显示详细信息：名称、大小、像素、拍摄时间、位置、拍摄参数等等的所有【相机拍摄的】元数据，要求对相机摄影的照片描述友好，参考那种专业的看图工具，没有的留空。此外图片的详情页需要支持修改当前图片的标签和修改地理位置，地理位置需要加一个小的地图组件，可以拖动点击位置，也可以输入经纬度。
- [ ] 支持设置界面在前端配置相应的配置项，比如配置oss的地址，域名，accesskey accesssercet等 env里的必要的配置项。
- [ ] 1.删除【首页】这个页面，默认显示【图库】页面，左边导航栏的顺序由上至下依次是：图库、相册、地图、标签、上传、分享、用户、设置。（普通成员没有【用户】管理页面）。2.然后【相册】页面需要做几个功能，第一行卡片是统计当前所有的照片数量，标签数量，活跃分享链接数量等等统计数据。接着下面是几种视图（两种视图切换按钮）——筛选视图：把地图页面上面的标签筛选功能拿过来，按照标签和照片的拍摄日期跨度筛选图片。时间线视图：左侧有一个时间线展示所有图片的拍摄时间，越靠上距离当前时间越近，越靠下拍摄时间越久。有照片的日期在左侧的时间线上要显示一个实心圆圈，并且在圆圈右侧有一个简单的时间标签，展示拍摄的年月日，对应当天的照片展示在这个时间节点的右侧。这几种视图的缩略图大小都相同，都展示成正方形，点击之后展开图片的详情页。
- [ ] 地图页面去掉上方的筛选功能，只默认显示该用户的所有图库中的图片在地图上，用（裁切的方形）缩略图展示。地图组件占满当前页面的上下高度，左右宽度自适应调节，右侧留现在的看图面板，当点击地图中某个图片的缩略图时，这个图片在地图右侧的看图面板中按原图原比例展示更大的缩略图，缩略图下方展示出该图片的元数据信息。点击看图面板中的缩略图时，放大展开到该图片的详情页。
- [ ] 支持用户之间的oss配置、标签、图库展示、上传、分享、地图等所有数据的隔离。该用户登录之后只能显示该用户的所有数据，不能查到其他用户的图片和标签等任何数据。管理员账户只支持对用户账号的【增、删、查、改】。如果管理员删除用户，则级联删除该用户的所有配置信息。

